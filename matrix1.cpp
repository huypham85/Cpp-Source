#include<bits/stdc++.h>
typedef long long ll;
using namespace std;
int t;
int a[1001][1001];
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);cout.tie(NULL);
    cin>>t;
    while(t--){
        int n, m;
        cin>>n>>m;
        for(int i=0;i<n;i++){
            for(int j=0;j<m;j++) cin>>a[i][j];
        }
        
    }
    return 0;
}